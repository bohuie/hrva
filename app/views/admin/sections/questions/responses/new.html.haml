= provide(:title, 'New Response') 

.page-header
  %h1
    Create New Response

.right
  = link_to 'Back to Questions Listing', admin_section_question_path( @section, @question )

%p
%br
%br

= simple_form_for( [:admin, @section, @question, @response] , :html=>{:class=>'form-horizontal'} ) do |f| 
  = f.error_notification 
  = render :partial => 'form', :locals=>{:f=>f}

