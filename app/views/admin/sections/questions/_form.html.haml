= f.input :item, 
  :input_html => {:rows=>6}

= f.input :qtype, 
  :label=> 'Question Type', 
  :collection => Question.qtypes.map{ |c| [c.to_s.humanize, c]}

= f.input :instruction, 
  :input_html => {:rows=>10}
 
%br
%br
%center
  = f.button :submit, :class => 'btn btn-primary'
