
%table.table.table-striped.table-bordered{:cellpadding=>10, :width=>'100%'}
  %tr
    %th
      Value
    %th
      Label
    %th
      Actions 

  - @responses.each do |response| 
    %tr
      %td
        = response.value
      %td
        = response.label
      %td{:width=>'20%'}
        %span{:style => 'white-space: nowrap'}
          .center 
            = action_links [:admin, @section, @question, response], :exclude => [:index], :delete => {:title=>'Delete', :alt_text=>'Delete', :confirm=> 'Are you sure you want to delete this response label?'}


