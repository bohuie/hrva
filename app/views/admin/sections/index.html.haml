= provide(:title, 'Sections Listing') 

.page-header
  %h1
    List of Sections

%table{:cellpadding=>10, :border=>1}
  %tr
    %th
      Title
    %th
      Description
    %th
      Actions 

  - @sections.each do |section| 
    %tr
      %td
        = section.title 
      %td
        = section.description 
      %td
        %span{:style => 'white-space: nowrap'}
          = action_links [:admin, section], :exclude => [:index], :delete => {:title=>'Delete', :alt_text=>'Delete', :confirm=> 'Are you sure you want to delete this section?'}

%br 

=# = link_to 'New Section', new_admin_section_path 
