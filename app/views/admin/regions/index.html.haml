= provide(:title, 'Regions Listing') 

.page-header
  %h1
    List of Regions
  
%table.table.table-striped.table-bordered{:cellpadding=>10, :width=>'100%'}
  %tr
    %th
      Label
    %th
      Number of Users
    %th
      Actions 

  - @regions.sort_by{|r| r.label}.each do |region| 
    %tr
      %td
        = region.label 
      %td
        = region.users.count
      %td{:width=>'20%'}
        %span{:style => 'white-space: nowrap'}
          .center
            = action_links [:admin, region], :exclude => [:index], :delete => {:title=>'Delete', :alt_text=>'Delete', :confirm=> 'Are you sure you want to delete this region?'}

%br 

= link_to 'Create New Region', new_admin_region_path 
