= provide(:title, 'Edit Question') 

.page-header
  %h1
    Edit Question

.right
  = link_to 'Show Question', question_path(@question)
  &nbsp;
  &nbsp;
  = link_to 'Back to Questions Listing', questions_path 
%p

= simple_form_for( @question ) do |f| 
  = f.error_notification 
  = render :partial => 'form', :locals=>{:f=>f}

